from rich import print as rprint


NUM_LIST = [1, 2, 3, 4]
rprint(NUM_LIST)


NUM_TUPLE = (1, 2, 3, 4)
rprint(NUM_TUPLE)

#This is using the block cursor which doesn't bother me as much as it used to earlier. This will never be done accoring to my wishes.
